query fetchPage($uri: ID!) {
  page(id: $uri, idType: URI) {
    id
    title
    content
    featuredImage {
      node {
        altText
        sourceUrl
      }
    }
    translations {
      id
      uri
      language {
        id
        code
      }
    }
  }
}
